# Snapshots

## Purpose

This directory is reserved for AI work snapshots.

Snapshots are short-term recovery artifacts and are separate from Git commits.

## Git Policy

Snapshot contents must not be committed to Git.

Only this README should be tracked so the directory purpose remains visible.

## Runtime Rule

Future Snapshot Manager tooling should write snapshot data here or to another ignored snapshot directory defined by `.gitignore`.

